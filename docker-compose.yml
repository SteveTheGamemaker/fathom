services:
  fathom:
    build: .
    container_name: fathom
    restart: unless-stopped
    ports:
      - "8989:8989"
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - FATHOM_DATABASE__PATH=/app/data/fathom.db
